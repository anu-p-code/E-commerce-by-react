import React from 'react';
import CartItems from '../CartItems/CartItems'; // Adjust the path as necessary

const Cart = () => {
    return (
        <div>
            <CartItems /> {/* Ensure CartItems component is properly imported */}
        </div>
    );
};

export default Cart;
